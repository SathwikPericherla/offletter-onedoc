import React from 'react'
import ReactDOM from 'react-dom/client'
import {PDFTemplate} from './pdf-template.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PDFTemplate />
  </React.StrictMode>,
)